# Vault Diff — 기획 메모

## 핵심 개념

vault의 "어제 상태"와 "지금 상태"를 비교해서 **내가 무엇을 했는지** 요약해주는 플러그인.

---

## 보여줄 수 있는 것들

| 항목 | 방법 |
|---|---|
| 새로 생긴 파일 | `TFile.stat.ctime` 기준 |
| 수정된 파일 | `TFile.stat.mtime` 기준 |
| 삭제된 파일 | `vault.on("delete")` 이벤트 캐싱 |
| 가장 많이 수정된 파일 | mtime + 파일 크기 변화량 |
| 기간별 비교 | 오늘 / 이번 주 / 이번 달 |

---

## UX 시나리오

```
📊 오늘의 Vault 변화 (2026-03-20)

새 파일 (3)
  + 회의록_0320.md
  + 아이디어_플러그인.md
  + 독서노트_파친코.md

수정된 파일 (7)
  ✎ PROGRESS.md         (+340자)
  ✎ 프로젝트_기획.md    (+120자)
  ✎ 일기_0320.md        (+80자)
  ...

삭제된 파일 (1)
  - 임시메모.md
```

---

## Vault Gardener와의 관계

- Vault Gardener → **방치된 파일** 발견 (과거 지향)
- Vault Diff → **오늘 내가 한 것** 파악 (현재 지향)

서로 보완 관계. 같은 플러그인 안에 탭으로 추가하거나 별도 플러그인으로 분리하는 방향 검토 필요.

---

## 기술적 고려사항

### 구현 가능한 것
- 새 파일: `TFile.stat.ctime` 기준으로 오늘 생성된 파일 필터링
- 수정된 파일: `TFile.stat.mtime` 기준으로 오늘 수정된 파일 필터링
- 파일 크기 변화: 이전 크기를 캐싱해두고 현재 `TFile.stat.size`와 비교

### 난관
- **삭제 추적**: `vault.on("delete")` 이벤트를 실시간으로 캐싱해야 함
- 플러그인이 꺼져 있던 동안의 삭제는 감지 불가

---

## 미결 사항

- [ ] Vault Gardener에 탭으로 통합 vs 별도 플러그인
